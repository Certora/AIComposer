A previous run of this workflow crashed. The following files were recovered from the in-progress virtual filesystem at the time of the crash. Use these as a starting point — they may be incomplete or contain errors:

{% for path, content in vfs_files %}
## {{ path }}
```
{{ content }}
```
{% endfor %}

Resume from where the previous run left off, fixing any issues in the recovered files. NB: These files in these versions are NOT yet placed onto the VFS, you will need to emplace
them yourself via `put_file`. While they may exist in the VFS, their contents do NOT necessarily match the contents shown above.
