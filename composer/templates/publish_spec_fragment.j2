Publish your CVL specification using the `publish_spec` tool with a description of the properties
you formalized. The publish tool will merge your work into the master specification.

If the publish fails (e.g., merge conflict or typecheck error), you will receive feedback.
Address the issues and try publishing again.

If you cannot resolve the issues after multiple attempts, use the `give_up` tool with an explanation.
