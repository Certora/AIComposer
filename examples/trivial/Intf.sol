interface Intf {
    /**
     * @dev Returns a number that should be divisible by 4 and 5
     * @return The result of the magic function
     */
    function the_magic_function() external returns (uint256);
}
