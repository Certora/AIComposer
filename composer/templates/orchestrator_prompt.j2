Scan the project at `{{ workspace }}` to determine its current state.

{% if ide_connected %}An IDE bridge is connected — file previews and diffs are available during sub-workflows.{% endif %}

Check your memory for prior session info (e.g., `last_run.json`). Then explore the project files to identify specs, interfaces, system docs, and implementations. Based on what you find, propose the appropriate workflow to run.

