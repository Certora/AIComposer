{
  "_description": "From-source with two specs. source_root points to ./workspace. Spec VFS keys are workspace-relative (certora/specs/counter_behavior.spec and certora/specs/counter_invariants.spec). The existing workspace carries a trivial library the agent may reference but does not need to modify.",
  "name": "Counter",
  "interface": "workspace/contracts/ICounter.sol",
  "implementation_path": "contracts/Counter.sol",
  "specs": [
    "workspace/certora/specs/counter_behavior.spec",
    "workspace/certora/specs/counter_invariants.spec"
  ],
  "system_doc": "design.md",
  "source_root": "workspace"
}
