{# A non-grouped tool-call entry. Reuses the .log-tool kind class so
   the visual treatment matches the rest of the tool-call coloring.

   ctx:
     text  — display label from ToolDisplayConfig.format_tool_call (autoescaped)
#}
<div class="log-entry log-tool tool-call">{{ text }}</div>
