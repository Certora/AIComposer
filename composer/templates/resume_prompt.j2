The workflow you are resuming was started with one or more specification files. When the initial
workflow was completed, you summarized your progress as follows:

```
{{ commentary }}
```

{% if spec_deltas %}
{% if spec_deltas|length == 1 %}
One specification file has since been updated. Carefully analyze the diff below and consider
what, if anything, in the current version of the implementation has been changed in response.
{% else %}
The following {{ spec_deltas|length }} specification files have since been updated. Carefully
analyze each diff and consider what, if anything, in the current version of the implementation
has been changed in response. Remember: every spec file must pass its own prover run.
{% endif %}

{% for d in spec_deltas %}
## Spec file at `{{ d.vfs_path }}`

**Original version:**
```
{{ d.orig_text }}
```

**Updated version (currently in the VFS at `{{ d.vfs_path }}`):**
```
{{ d.updated_text }}
```

{% endfor %}
{% else %}
No spec files have been changed on this resume.
{% endif %}

{% for change in other_changes %}
In addition, the {{ change.single_form }} used in the original workflow was the following:

```
{{ change.orig_text }}
```

However, the {{ change.single_form }} has since been updated to the following:

```
{{ change.updated_text }}
```

{% if change.vfs_note %}
This updated {{ change.single_form }} has already been changed in the VFS at path `{{ change.vfs_note }}`.
{% endif %}

In addition to the above changes, you must also carefully analyze the differences between the {{ change.plural }}.
Identify what has been added, removed or changed, and consider what, if anything, in the current version
of the implementation has been changed.
{% endfor %}

{% if spec_change_commentary %}
The following is provided as commentary on the input changes, which can include the reasons for the
changes or a summary thereof:

{{ spec_change_commentary }}
{% endif %}
