Scan the project at `{{ workspace }}` to determine its current state.

Check your memory for prior session info (e.g., `last_run.json`). Then explore the project files to identify specs, interfaces, system docs, and implementations. Based on what you find, explain what you see and suggest the appropriate workflow.
